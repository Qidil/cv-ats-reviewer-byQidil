# antislop audit 002 (2026-09-25): follow-up to audit 001

**Scope:** the fixes the owner approved on 2026-09-25 ("fix" plus decisions D1 to D8) for the findings in `audit-001-2026-09-25.md`.

## Result per finding

| # | Status | Evidence |
| --- | --- | --- |
| 1 | Resolved | `text-muted` is now #8A93A3: 6.19 / 5.73 / 4.74:1 on app / surface / elevated (`contrast-check.py`). Playwright reads `rgb(138, 147, 163)` on `/`. |
| 2 | Resolved | Em dashes in `PRD.md` BR-08 replaced with colons; no em dash left in `project-context/`, `app/`, `lib/`, or `types/`. |
| 3 | Resolved by owner decision | Inter stays until the UI revision after the functional phases; rationale recorded in `StyleGuide.md` § Current Visual Direction. |
| 4 | Resolved by owner decision | Dark only for now; same section records the reason and the planned revision. |
| 5 | Resolved | "Lightning-fast", "robust", "Seamless", and "executive-grade" removed from the specs. |
| 6 | Resolved | New `border-strong` token #6B7280 (3.96 / 3.67 / 3.04:1) for form controls; `border-subtle` limited to dividers, with measured values in `StyleGuide.md`. |
| 7 | Resolved | Temporary `app/icon.svg` (owner decision D3); the page loads with no console errors and no failed requests at 1440 px and 390 px. |
| 8 | Deferred by owner decision | Dials are set in the UI revision; recorded in `StyleGuide.md`. |
| 9 | Deferred by owner decision | Icon set is reviewed in the same UI revision; recorded in `architecture.md` § Tech Stack. |

## Mode going forward

UI and copy work now applies antislop while it is written (owner decision D8), and each phase still ends with an audit (`AGENTS.md` Rule 2).
