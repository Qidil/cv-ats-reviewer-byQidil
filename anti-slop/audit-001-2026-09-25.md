# antislop audit 001 (2026-09-25)

**Scope:** Phase 1 output: `app/layout.tsx`, `app/page.tsx`, `app/globals.css`, code comments in `types/` and `lib/db/`, and the Phase 1 docs in `project-context/`.
**Mode:** 2 (after the phase, per `AGENTS.md` Rule 2). Nothing below has been fixed yet; fixes wait for the owner to approve numbers.
**Direction source:** `project-context/StyleGuide.md` (no `DESIGN.md`). The page is a scaffold placeholder, not a shippable screen.

## Findings

| # | Priority | Rule | Finding |
| --- | --- | --- | --- |
| 1 | HIGH | R-25 | `text-muted` (#6B7280) measures 3.96:1 on `bg-app`, 3.67:1 on `bg-surface`, 3.04:1 on `bg-surface-elevated` (contrast-check.py). It fails 4.5:1 for normal text and is already used for the status line on `/`. StyleGuide claims 4.6:1. |
| 2 | HIGH | R-02 | Em dashes in `project-context/PRD.md` lines 103 and 104. |
| 3 | MEDIUM | R-06 | Inter is set by StyleGuide with no written brand reason. |
| 4 | MEDIUM | R-21 | The theme is dark-only with no written reason and no light mode. |
| 5 | MEDIUM | R-16 | Spec prose uses buzzwords: "Lightning-fast" (`architecture.md`), "robust" (`architecture.md`), "Seamless" (`PRD.md`), "executive-grade" (`StyleGuide.md`). |
| 6 | LOW | R-25 (non-text) | `border-subtle` (#374151) measures 1.86:1 on `bg-app`; StyleGuide claims 3.2:1. Fine for decorative dividers, too weak for input borders. No 3:1 border token exists for form controls. |
| 7 | LOW | R-35 | Console shows one error on every load: `GET /favicon.ico 404`. No app icon exists; creating one needs the owner's direction (R-23). |
| 8 | LOW | Part 3 | ENERGY / RHYTHM / MOTION dials are not declared in StyleGuide. Not needed for the scaffold; required before Phase 4 UI work. |
| 9 | LOW | R-04 | `lucide-react` is installed per the Phase 1 plan but unused. Before Phase 4, decide whether its look suits the product or pick another set. |

## Delivery Gate (applicable items)

- R-02 FAIL: finding 2 (spec docs only; no em dash in UI text or code).
- R-03 PASS: 390 px and 1440 px viewports, `scrollWidth` equals viewport width, heading fits one line.
- R-17, R-18, R-36, R-38 PASS: no numbers, testimonials, or claims on the page; the page states the upload feature is not available yet.
- R-23 PASS: no logo or avatar created; product name shown as text.
- R-24, R-26 PASS (N/A): no navigation and no interactive elements.
- R-25 FAIL: finding 1.
- R-32 PASS: global `:focus-visible` outline (2 px #3B82F6, 5.21:1 on `bg-app`); no `outline: none`.
- R-35 PARTIAL: `next build`, `next start`, and `next dev` all served the page; console has the favicon 404 (finding 7).
- Code comments (antislop-code checklist) PASS: every comment states a reason, contract, or edge case; no banners, narration, or empty labels.
