# antislop audit 006 (2026-09-26): Phase 3 gate fixes

**Scope:** follow-up to audit 005 after the owner approved the Phase 3 gate fixes. Covers the new user-facing string (`TOO_MANY_REQUESTS` in `lib/api/errors.ts`), the owner-facing text added to `project-context/credentials.md` (§1 comments, §4, §6 item 3, §8), and every code comment added or changed by DELTA-22 to DELTA-25 and AS-CODE-02 (`lib/ai/quota.ts` including `clientNetwork`, `lib/ai/orchestrator.ts`, `lib/ai/parser.ts`, `lib/api/analyze.ts`, `lib/api/config.ts`, `types/api.ts`, and their tests).
**Mode:** 2 (end of the fix round). Findings 2 and 3 of audit 005 were fixed with the owner's approval; nothing new was fixed without it.
**Not applicable:** `antislop-ui`, `antislop-human`, `antislop-layoutmobile` (no UI in Phase 3).

## Verdicts on audit 005

| # (005) | Verdict | Evidence |
| --- | --- | --- |
| 1 | Resolved earlier | `cleanAiText` and its tests are unchanged. |
| 2 | Resolved | The `cleanAiText` doc comment now states only the constraint: free models do not always follow the prompt's style rules, so the text is rewritten before users see it. |
| 3 | Resolved | `EXPIRY_MARGIN_S`: "EXPIREAT is absolute, so an hour of slack keeps a store whose clock runs ahead from dropping a count before its window ends." The same margin now also covers the hourly counter. |
| 4 | Unchanged (INFO) | `INVALID_INPUT` still lists three checks; Phase 4 validates each field in the browser. |

## New findings

| # | Priority | Rule | Finding |
| --- | --- | --- | --- |
| 1 | INFO | R-15 | `TOO_MANY_REQUESTS` ("Jaringan Anda sudah mencapai batas analisis per jam, dengan kunci bawaan maupun API key sendiri. Coba lagi mulai jam berikutnya.") says what happened, that a personal key does not help, and when to retry. It names no number, because `HOURLY_REQUEST_LIMIT` can change; the exact time comes from `resetsAt` (StyleGuide §7.5). |
| 2 | INFO | antislop-code | `DAILY_QUOTA_EXCEEDED` still hardcodes "10" while `DAILY_ANALYSIS_LIMIT` is configurable (known since audit 005's scope; left for the Phase 4 copy review together with the notice UI). |

## Delivery Gate (applicable items)

- R-02 PASS: no em dash in `lib/`, `app/`, `types/`, `anti-slop/`, the edited specs, or `credentials.md` (scan: 0 hits); the catalog test still asserts it for every message, including the new one.
- R-16 PASS: no buzzwords in the new message or the credentials text.
- R-17, R-38 PASS: the credentials text states facts from the Upstash docs (Developer API keys authenticate with the account email as the username; keys are deleted under Account, Developer API) and the app's own behavior (about 5 store commands per analysis with the built-in key, 2 with a personal key).
- Error message PASS: the new message has a cause and a next step, matching the other 429 messages.
- Code comments checklist PASS: the new comments give a reason or a limit (why a 400 moves on, why an abort is judged by which limit fired, why the document is sized before the AI call, why a store outage skips the hourly limit). No history, narration, banners, or vague TODOs. One test comment explains why the refused request carries a non-PDF file (reaching extraction would answer `PDF_INVALID`).
