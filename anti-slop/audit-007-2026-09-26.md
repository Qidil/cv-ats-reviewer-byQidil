# antislop audit 007 (2026-09-26): Phase 4 dashboard UI, copy, and comments

**Scope:** the Phase 4 interface (`app/[lang]/`, `components/**`, `app/globals.css`), the English and Indonesian UI strings (`lib/i18n/dictionaries/*`), the bilingual server messages (`lib/api/errors.ts`), the prompts that shape the AI text users read (`lib/ai/prompts.ts`), `README.md`, `.env.example`, and every code comment in the files changed since `dff3d5f`.
**Mode:** 2 (end of phase, `AGENTS.md` Rule 2), after mode 1 was applied while building (owner decision D8). Nothing below has been fixed yet; fixes wait for the owner's approval. Bracketed IDs (G-xx) match the unified Phase 4 gate report.
**Direction (R-37):** `project-context/StyleGuide.md` as written. Theme, typeface, icon set, and the liveliness dials are deferred to the visual revision by owner decision D7 (StyleGuide.md:23, phase 4 plan "Out of scope"), so Block 3 is recorded as deferred, not failed. Design Read: an Operate-mode tool for job seekers checking a CV, dark token system from the StyleGuide; the current build reads as ENERGY 1 / RHYTHM 1 / MOTION 1.
**Evidence:** live Playwright session on the production build (BUILD_ID 20:41:53) at 320, 390, and 1440 px (820 px in the Task 4.12 run); contrast computed with `.opencode/skills/antislop-human/contrast-check.py`; Impeccable detector (`impeccable detect --json components app/[lang] app/globals.css`); source reads. Screenshots: `.playwright-mcp/phase4-gate-320-*.png`, `.playwright-mcp/phase4-delta49-jobs.png`.

## Delivery Gate

### Block 1: Hard Gate

- R-02 PASS: no U+2014 in `app/`, `components/`, `lib/`, `types/`, `README.md`, or `.env.example`; the escaped form appears only in the cleaner, the tokenizer, and their tests.
- R-03 FAIL: tap targets below 44 px (#1); a model ID without break points overflows the result page at 320 px (#6). No other horizontal overflow at 320, 390, or 1440 px in the start, file error, loading, result, Settings, and History states.
- R-17, R-18, R-38 PASS: no statistics, testimonials, or invented content; every score comes from the analysis.
- R-23 PASS: no generated logo or avatar; the brand is the text "CV ATS".
- R-24 PASS: the header holds the language link, History, and Settings, and each one works.
- R-25 PASS: lowest text pair `#8A93A3` on `#1F2937` = 4.74:1; error red on the page 5.09:1 and on `#111827` 4.71:1; white on the primary button 5.17:1. Non-text: focus ring 5.21:1, input border 3.04:1 on the dialog and 3.96:1 on the field.
- R-26, R-35 PASS: clicked Choose a file, a non-PDF upload (message under the dropzone, focus kept on the picker), Analyze CV, Cancel (silent return, focus on submit), Upload another CV (focus on the picker), Settings (Escape closes, focus returns to the trigger), History (opens, closes), and the language switch (locked during analysis). Console: 0 errors, 0 warnings.
- R-27 PASS: empty form, loading (progress card with elapsed time and skeletons), file error, restoring, and the History empty and off states.
- R-28 N/A (no FAQ). R-33 PASS. R-34 N/A (one theme, from the StyleGuide).
- R-32 PASS with a note: tab order follows the visual order, no positive `tabindex`, the ring shows on `:focus-visible`, dialogs close with Escape. Focus falls to `<body>` while an analysis runs (#5).
- R-36 FAIL: the Settings sentence "The key stays in this browser and is sent only with your analyses, to that provider." leaves out that the key passes through this app's server (#2).
- R-37 PASS: direction is the StyleGuide; dials deferred by owner decision D7.

### Block 2: Purpose-Gate

- R-01, R-07, R-13 PASS: no gradients, background patterns, or glow.
- R-04 NOTE: `lucide-react` icons (plan T9); each icon matches its action (history, settings, upload, delete). The icon set belongs to the D7 revision.
- R-06 NOTE: Inter from the StyleGuide; the typeface belongs to the D7 revision.
- R-09 PASS: badges carry real status (priority, score, match).
- R-10 PASS: blur on two elements (sticky header, dialog backdrop), at the cap.
- R-12 PASS: no decorative shadows.
- Quote box left border (detector `side-tab` at `components/results/lists.tsx:56`) PASS: StyleGuide §7 "Quote Box" asks for a subtle left border to mark verbatim CV text, drawn in the neutral `border-strong`.
- R-19 PASS: one staggered fade when the result appears and a pulse while loading; every animated component reads `useReducedMotion`.

### Block 3: Liveliness

Deferred to the visual revision (owner decision D7).

### Block 4: Craftsmanship and Quality Locks

- C-2, C-4: see #1, #5, #6.
- C-5: see #2.
- R-15 PASS: specific CTAs ("Analyze CV", "Upload another CV", "Test key", "Try again").
- R-16 PASS: no buzzwords in the UI, server messages, prompts, or README.
- R-11, R-29 PASS: radius scale from the StyleGuide; palette of neutrals, action blue, and the three status colors.

## Findings

| # | Priority | Rule | Finding | Gate ID |
| --- | --- | --- | --- | --- |
| 1 | HIGH | R-03 | Tap targets under 44 px: language link 44x40 at every width; History and Settings 40x44 below 640 px; dialog Close and History delete 40x44 at every width. | G-01 |
| 2 | HIGH | R-36, C-5 | The Settings privacy sentence overstates: every analysis sends the key to this app's server, which forwards it (`en.ts:118`, `id.ts:117`). The key test is the only direct browser-to-provider call. | G-02 |
| 3 | MEDIUM | R-38 | `README.md:13-14` still says "5 to 10 roles" after DELTA-49. | G-07 |
| 4 | LOW | C-4 | At 1440x900 the page grows from 900 to 938 px when an analysis starts, a scrollbar appears, and the header shifts 7 px, then back after Cancel. | G-11 |
| 5 | LOW | R-32 | Focus falls to `<body>` during the analysis. | G-12 |
| 6 | LOW | R-03 | History row at 320 px: text column 71 px, metadata on 6 lines (142 px and 3 lines at 390 px). A model ID without break points overflows the page at 320 px (`result-view.tsx:80`). | G-13 |
| 7 | LOW | copy | Messages that point to the wrong fix or promise a feature that is off: `NETWORK_TIMEOUT`, `storageBlocked`, `INVALID_INPUT` (catalog and PRD text). | G-18 |
| 8 | LOW | copy | Indonesian that reads translated or mixes terms; English polish in the rubric text and README; unused keys (list below). | G-19 |
| 9 | LOW | copy | `cleanAiText` leaves broken sentences ("For example, Change the bullet."). | G-20 |
| 10 | LOW | copy | Prompt style rules also cover the verbatim snippet; no rule for "Anda" or native writing; "jobs/pekerjaan" where the UI says "roles/posisi". | G-17 |
| 11 | LOW | antislop-code | Comments that carry history or phase references, one wrong, one misplaced (list below). | G-21 |
| 12 | INFO | detector | `broken-image` at `components/preview/page-images.tsx:26` is a false positive: the ref callback sets the object URL, and the page images rendered live. | - |
| 13 | INFO | fixtures | `lib/pdf/extractor.test.ts:156, 248, 266` use "Budi Sudirman" (Phase 2), not "Budi Santoso". | - |

### Copy replacements (G-18, G-19, G-20)

| Where | Current | Proposed |
| --- | --- | --- |
| `README.md:13-14` | "also lists 5 to 10 roles that fit it" | "also lists the 5 roles that fit it best." |
| `en.ts:118` / `id.ts:117` | "The key stays in this browser and is sent only with your analyses, to that provider." | EN: "The key is saved in this browser only. Each analysis sends it to this app's server, which passes it to that provider without storing it." ID: "Kunci hanya disimpan di browser ini. Setiap analisis mengirimnya ke server aplikasi ini, yang meneruskannya ke provider tersebut tanpa menyimpannya." |
| `en.ts:77` / `id.ts:76` (PRD §6.3 text) | "History lasts only while this tab is open." | EN: "Local storage is blocked, often by private browsing. Results show until you leave this page and are not saved to History." ID: "Penyimpanan lokal diblokir, biasanya oleh mode privat. Hasil tampil sampai Anda meninggalkan halaman ini dan tidak tersimpan di Riwayat." |
| `errors.ts:125-126` (api.md text) | "...took longer than 120 seconds. Check your internet connection..." | EN: "The AI did not finish the analysis within 120 seconds. Try again in a moment." ID: "AI belum selesai menganalisis dalam 120 detik. Coba lagi sebentar lagi." |
| `errors.ts:19-20` (api.md text) | base message asks to check "the model and address in Settings" | End the base message after the job description; move the Settings part into a personal-key message. ID: "Data analisis" becomes "Permintaan analisis". |
| `id.ts:91` with `languageNames` | "Hasil ini ditulis dalam English." | In-sentence names: en `{ en: "English", id: "Indonesian" }`, id `{ en: "bahasa Inggris", id: "bahasa Indonesia" }`; update `dashboard.test.tsx:178`. |
| `en.ts:84` / `id.ts:83` | "has sent the most analyses allowed this hour" / "sudah mengirim analisis sebanyak batas jam ini" | EN: "Your network has reached this hour's analysis limit. The limit also applies to your own API key. Try again at {time}." ID: "Jaringan Anda sudah mencapai batas analisis untuk jam ini. Batas ini juga berlaku untuk API key Anda sendiri. Coba lagi pukul {time}." |
| `errors.ts:50, 94, 74` | "penyedia AI"; "termasuk setelah dilanjutkan"; "sedang penuh" | "provider AI"; "bahkan setelah dilanjutkan"; "sedang ramai" |
| `id.ts:22, 42, 106, 149` | "dapatkan" (twice); "Keahlian untuk ditambah"; "alamat OpenAI-compatible" | "lalu lihat skor beserta baris yang perlu diubah"; "dan lihat 5 posisi yang paling cocok"; "Keahlian yang perlu ditambahkan"; "alamat yang kompatibel dengan OpenAI" |
| `en.ts:59, 61` / `id.ts:58, 60` | `actions.chooseAnotherFile`, `actions.dismiss` | Delete (unused). |
| `README.md:17, 35`, bullets `:13-21` | "each quoting the CV text it refers to"; "(never on a guess)"; bold lead-ins | "quoting the CV line they refer to when there is one"; "(a key the app cannot place is refused)"; plain bullets |
| `lib/ats/rubric-text.ts:182, 112, 135` | "other ATS systems"; "does not list yet:"; "whose numbers could be checked" | "an ATS can still read it"; "Not in the Skills section yet: {list}."; "No bullet points to check for numbers." |
| `lib/ai/parser.ts:78-79, 92-95` | label rewritten to "For example, Change..." / "such as for example, ..." | Drop the label at a sentence start and after "such as" / "seperti"; update `parser.test.ts:89, 91, 101, 103`. |
| `lib/ai/prompts.ts:100, 159` and `:98-104, 157-163` | style rules cover every field; no address rule | Add: the rules cover every field except `targetTextSnippet`, which stays exactly as in the CV. ID: address the user as "Anda" and write natural Indonesian. Use "roles" / "posisi" at `:59, 109, 118, 168`. |

### Comment replacements (G-21, comments only)

| Where | Current | Action |
| --- | --- | --- |
| `lib/ai/providers.ts:24` | "Values the old engine used: ..." | "A low temperature keeps scores stable; the token cap leaves room for Mode B's longer JSON." |
| `lib/ai/providers.ts:76` | "Unchanged from Phase 3 (...)" | "OpenRouter status mapping (rules.md §4.2.3, §4.2.4)." |
| `types/db.ts:36` | "...the Phase 5 highlights need..." | "Everything the page preview and snippet highlights need; read only when a CV is opened." |
| `lib/pdf/types.ts:93` | "Phase 5 maps AI snippets back to boxes with it." | "rawText.slice(textStart, textEnd) === text, which maps an AI snippet back to its boxes." |
| `lib/api/analyze.ts:241` | "...the Inspector still has the text..." | "The runs are dropped first: the text still arrives, only the boxes do not." |
| `lib/ai/prompts.ts:11` | "The language of every text field in the answer" | "Output language of every text field except targetTextSnippet (BR-13)." |
| `components/settings/byok-modal.tsx:51` | "Error text uses red-400: ..." | Move above `:289`, text unchanged. |
| `components/test-utils/fixtures.ts:6` | "A small but complete analysis..." | "A trimmed analysis of the fictional "Budi Santoso" CV (3 checks, 2 roles) for component tests." |
| `components/test-utils/render.tsx:9-13` | three lines with version history | "Browser APIs jsdom lacks. The dialog stand-ins copy only what ui/dialog.tsx uses: the open attribute and the close event." |
| `lib/api/analyze.ts:269-273` | step list | "One request per analysis (P3-D3): the quota is checked before any work and counted only on success; the AI sees visibleText only." |
| `lib/api/analyze.ts:35, 39` | `/** P3-D5(b). */`, `/** P3-D5(c). */` | "P3-D5(b): request field caps." / "P3-D5(c): extraction's share of the 180 s route." |
| `lib/ai/providers.ts:8, 49`; `types/ats.ts:55` | verification and decision dates | Optional: drop the dates, keep the facts. |
