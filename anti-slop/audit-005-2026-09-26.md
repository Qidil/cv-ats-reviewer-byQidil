# antislop audit 005 (2026-09-26): Phase 3 copy, prompts, AI output, and comments

**Scope:** the Indonesian user-facing strings added in Phase 3 (error catalog in `lib/api/errors.ts`, `PDF_TOO_LARGE` and `PDF_TOO_COMPLEX` in `lib/pdf/types.ts`), the prompts in `lib/ai/prompts.ts` (they shape the AI text users read), the AI output of two live runs on the owner's CV, the owner-facing how-to sections added to `project-context/credentials.md` (§3 and §4), and every code comment in the new and changed files.
**Mode:** 2 (end of phase, `AGENTS.md` Rule 2), after mode 1 was applied while writing (owner decision D8). Nothing below has been fixed yet; fixes wait for the owner's approval.
**Not applicable:** `antislop-ui`, `antislop-human`, `antislop-layoutmobile` (Phase 3 changes no UI).

## Findings

| # | Priority | Rule | Finding |
| --- | --- | --- | --- |
| 1 | MEDIUM (resolved during the phase) | R-36, BR-07 | The live Mode B run wrote "Contoh:" in 5 of 44 AI texts although the prompt forbids it. `cleanAiText` now rewrites such labels, markdown emphasis, and em dashes on the server; covered by tests. |
| 2 | LOW | antislop-code | The `cleanAiText` doc comment carries history ("a live Mode B run wrote ... five times"); the comment should state the constraint only. |
| 3 | LOW | antislop-code | The `EXPIRY_MARGIN_S` comment says the margin keeps "clock skew at midnight" from reviving an old count. The key already changes with the WIB date; the margin only keeps a store whose clock runs ahead from expiring a count before its day ends. |
| 4 | INFO | R-15 | `INVALID_INPUT` lists three things to check in one message. Acceptable as a fallback, because Phase 4 validates each field in the browser with its own message. |

## Delivery Gate (applicable items)

- R-02 PASS: no em dash in `lib/`, `app/`, `types/`, or `anti-slop/` (scan), and tests assert it for every catalog message and both system prompts.
- R-16 PASS: no buzzwords in messages, prompts, or the how-to sections.
- R-17, R-36, R-38 PASS: the prompts forbid invented numbers and ask for `[jumlah]`/`[persen]` placeholders; the how-to sections state only facts from the Upstash and Vercel docs (free tier limits, default region) and the app's own behavior.
- BR-07 and StyleGuide § Anti-AI Slop Rules: live Mode A had 0 labels, bold markers, em dashes, or greetings in 31 AI texts; live Mode B had 5 "Contoh:" labels before the server cleanup (finding 1).
- Error messages PASS: each says what happened and what the user can do next (wait, retry, use a personal key, or re-export the PDF).
- Code comments checklist PASS apart from findings 2 and 3: no banners, narration, emoji, or vague TODOs; comments state a reason, a limit, or a protocol detail.
