# antislop audit 004 (2026-09-26): follow-up to audit 003

**Scope:** the fixes the owner approved on 2026-09-26 ("lanjut fix" plus decisions P2-D5 amended and P2-D9) for the findings in `audit-003-2026-09-26.md`, and every Indonesian string written during those fixes.

## Result per finding

| # | Status | Evidence |
| --- | --- | --- |
| 1 | Resolved | Mode B treats keyword and skills as deferred to the AI, so no weakness or suggestion mentions a job description (`rubric.test.ts` "keeps the Mode B checks out of weaknesses and suggestions"). On the owner's CV, Mode B now suggests only the quantified check and typography. |
| 2 | Resolved | Formatting and readability both say "panjang CV di luar 50 sampai 1.200 kata" (`rubric.test.ts` "states the length limit in words"). |
| 3 | Resolved by owner decision | P2-D9: any font outside Arial, Calibri, and Helvetica gets "Pakai Arial, Calibri, atau Helvetica". The claim "lebih aman dibaca ATS" no longer exists under `lib/` (grep). |
| 4 | Resolved | The `HIDDEN_CONTRAST_MAX` comment now states that the limit falls between grey 229 and 230. |
| 5 | Resolved | The placement weights comment now reads "ported from the n8n engine's ATS reference". |
| 6 | Resolved | The `walkOperatorList` doc now says it returns every painted shape with its paint order. |
| 7 | Open, Phase 4 | "Mode B" and "Keamanan Parsing" stay until the Phase 4 UI copy review. |

## Strings added by the fixes

- "Deskripsi pekerjaan terlalu pendek untuk menilai cakupan keahlian."
- "Mudah dibaca ATS: ada email, nomor telepon, bullet point, dan panjang CV wajar." and "Mudah dibaca: ada ringkasan, bullet point ringkas, dan panjang CV wajar."
- "CV memakai {n} font: {daftar}. Pakai satu font untuk seluruh CV, lalu bedakan judul dengan ukuran dan huruf tebal."
- "Pakai Arial, Calibri, atau Helvetica" / "CV memakai font {daftar}. Ganti dengan salah satu dari Arial, Calibri, atau Helvetica."
- "Jarak antarbaris CV terlalu rapat" or "renggang", followed by "Atur spasi baris ke 1,0 sampai 1,15 di aplikasi pembuat CV."

Checked against R-02 (no em dash; grep under `lib/` finds none), R-16 (no buzzwords), R-17 and R-36 (no invented facts or claims; every number comes from the rule or the measured CV), and BR-07 (each suggestion opens with a direct instruction). Result: PASS.

## Code comments added by the fixes

Every new comment states a reason, unit, or edge case in one or two lines (for example why bars need a stack, why the lower quartile measures line spacing, and why square brackets become parentheses in `sanitizedText`). No banners, narration, or TODOs. Result: PASS.
