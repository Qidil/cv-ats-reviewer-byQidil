# antislop audit 008 (2026-09-26): Phase 4 gate fixes

**Scope:** follow-up to audit 007 after the owner approved every Phase 4 gate finding ("G5. A, G10. A, G22. setuju, G24. jam resetnya ikut GMT+8 saja, lanjut fix semua"). Covers the strings added or rewritten in this round (dictionaries, the error catalog, the prompts, README), the button variants and narrow-screen layout, and every code comment added or changed by DELTA-50 to DELTA-54 and the fix manifest.
**Mode:** 2 (end of the fix round). Every finding of audit 007 was fixed with the owner's approval; nothing outside the approved list was changed.
**Evidence:** production build (BUILD_ID 23:30:49) served by `npm run start`, Playwright at 320 and 1440 px on `/en` and `/id`, one live general review on the test CV (counts only), `rtk rg` scans, 672 tests passing three runs in a row.

## Verdicts on audit 007

| # (007) | Verdict | Evidence |
| --- | --- | --- |
| 1 | Resolved | New `icon` button variant (`min-w-11`), language link `min-h-11`. Playwright at 320 px: no control under 44 x 44 px on the start page, the result page, History (Close, delete), or Settings (Close 44 x 44). |
| 2 | Resolved | Settings intro, both languages: "The key is saved in this browser only. Each analysis sends it to this app's server, which passes it to that provider without storing it." Checked live on `/id`. |
| 3 | Resolved | README: "also lists the 5 roles that fit it best", bold lead-ins removed, "(a key the app cannot place is refused)". |
| 4 | Resolved | `scrollbar-gutter: stable` on `html`. At 1440 x 900 the header content stays at 73 px before and during an analysis (it moved from 80 to 73 before). |
| 5 | Resolved | Focus moves to the progress card (`role="group"`, named by its status line) in a layout effect; live: "Analyzing your CV" focused one second after submit. |
| 6 | Resolved | History drawer full width with the score badge wrapping under the details: text column 187 px (was 71), details on 2 lines (was 6). The model line breaks long IDs: an unbroken 70-character ID no longer overflows at 320 px. |
| 7 | Resolved | `NETWORK_TIMEOUT`, `storageBlocked`, and `INVALID_INPUT` (with a personal-key message) rewritten in `api.md` and PRD §6.3 first, then in code; all 14 new catalog strings match `api.md` word for word. |
| 8 | Resolved | Copy table of audit 007 applied: "Hasil ini ditulis dalam bahasa Inggris." (live on `/id`), "lihat", "Keahlian yang perlu ditambahkan", "provider AI", "bahkan", "ramai", the hourly notice, rubric and README polish, two unused keys deleted. |
| 9 | Resolved | `cleanAiText` removes the label before a whole sentence and after "such as" / "seperti": "Change the bullet.", "Use terms such as TypeScript.", "Pakai istilah seperti TypeScript."; a lowercase continuation keeps "For example," / "Misalnya,". |
| 10 | Resolved | Both prompts exempt `targetTextSnippet` from the writing rules, ban filler words, and say roles / posisi; the Indonesian prompt asks for "Anda" and natural Indonesian. |
| 11 | Resolved | All 12 comment replacements applied, including the optional date removals. |
| 12 | Unchanged (INFO) | Detector `broken-image` remains a false positive. |
| 13 | Unchanged (INFO) | `lib/pdf/extractor.test.ts` still uses "Budi Sudirman"; outside the approved list. |

## New strings in this round

| Where | Text | Check |
| --- | --- | --- |
| `results.failoverNote` | "The first model could not finish, so the next one in the chain wrote this result." / "Model pertama tidak bisa menyelesaikan analisis, jadi model berikutnya di rantai yang menulis hasil ini." | Matches PRD §6.3; says what happened without alarm (StyleGuide §7.7). |
| `results.continuationNote` | "The AI's answer was cut off once, so it was continued to finish the report." / "Jawaban AI sempat terpotong, lalu dilanjutkan sampai laporannya lengkap." | Matches PRD §6.3. |
| `errors.keyUnsendable` | "The saved API key contains characters that cannot be sent. Enter it again in Settings." / "API key yang tersimpan berisi karakter yang tidak bisa dikirim. Masukkan ulang di Pengaturan." | Matches `api.md` § Client Handling; cause plus next step. |
| `settings.keyUnsendable` | "The key contains characters that cannot be sent, often a hidden space from copying. Paste it again." / "Kunci berisi karakter yang tidak bisa dikirim, biasanya spasi tersembunyi dari hasil salin. Tempel ulang kuncinya." | Names the likely cause in plain words. |
| Catalog personal-key messages | `QUOTA_CHECK_FAILED`, `SERVICE_NOT_CONFIGURED`, `INVALID_INPUT` | Each tells a personal-key user what they can do; no internal terms. |
| `quotaTime` | "{time} GMT+8" | 24-hour clock in both languages (DELTA-50); live resets end in `+08:00`. |

## Delivery Gate (changed items)

- R-02 PASS: no U+2014 in `app/`, `components/`, `lib/`, `types/`, `README.md`, `.env.example`, or this folder's audit 007 (scan: 0 hits).
- R-03 PASS: tap targets at least 44 x 44 px everywhere checked; no horizontal overflow at 320 px, including a long model ID.
- R-25 PASS: bordered text buttons use `text-primary` on transparent over `bg-app` (18.33:1, computed with `contrast-check.py`) with the `border-strong` edge (3.96:1 against the page); icon buttons keep `text-secondary` (7.54:1).
- R-26, R-35 PASS: build run and served; live clicks: History open, row open, Settings open and Close, Upload another CV, Choose a file, Analyze CV; a cross-site POST refused with 400 `INVALID_INPUT` in the requested language; console 0 errors, 0 warnings.
- R-32 PASS: focus lands on the progress card during an analysis and on the result heading afterwards, also when the result view loads on demand.
- R-36 PASS: the Settings key sentence now matches the data flow.
- R-19 PASS: the skeleton pulse is a CSS animation (`animate-skeleton`, `motion-reduce:animate-none`), same 1.6 s, 0.4 to 0.8 opacity.
- Code comments PASS: the new comments give a reason (why redirects are refused, why an abort cannot wait for DNS, why a closed connection must settle, why the restore yields to the user, why the gutter is reserved); no narration, banners, or vague TODOs.
