# antislop audit 003 (2026-09-26): Phase 2 strings and comments

**Scope:** the Indonesian user-facing strings added in Phase 2 (`lib/pdf/types.ts` PDF error messages; `lib/ats/rubric.ts` check names, details, suggestions, typography and hidden-text notices) and every code comment in `lib/pdf/` and `lib/ats/`, tests included.
**Mode:** 2 (end of phase, `AGENTS.md` Rule 2), after mode 1 was applied while writing (owner decision D8). Nothing below has been fixed yet; fixes wait for the owner's approval.
**Not applicable:** `antislop-ui`, `antislop-human`, `antislop-layoutmobile` (Phase 2 changes no UI).

## Findings

| # | Priority | Rule | Finding |
| --- | --- | --- | --- |
| 1 | MEDIUM | R-36 | Mode B has no job description, yet its two top suggestions tell the user to copy terms "dari deskripsi pekerjaan" (reported in the code review as CR-18-03). |
| 2 | LOW | R-17, R-36 | "panjang dokumen di luar 1 sampai 2 halaman" names a page count, but the rule counts words (50 to 1,200). Readability says "panjang dokumen tidak wajar" for the same rule. |
| 3 | LOW | R-36 | "Arial, Calibri, atau Helvetica lebih aman dibaca ATS" has no evidence behind it: an ATS reads the text layer, and the font family does not change that layer. The rule comes from the n8n ATS reference, so the wording is the owner's call. |
| 4 | LOW | antislop-code | The `HIDDEN_CONTRAST_MAX` comment says 1.25:1 "matches the old RGB > 230 threshold". The boundary sits between grey 229 and 230, so grey 230 is now hidden where the old rule kept it visible. |
| 5 | LOW | antislop-code | "(ats-reference.md, Pattern F)" in `rubric.ts` points at a file that exists only in the n8n project. |
| 6 | LOW | antislop-code | The `walkOperatorList` doc says it returns "the shapes painted before them"; it returns every painted shape with its paint order. |
| 7 | INFO | R-16 | "Mode B" and "Keamanan Parsing" are internal or technical terms inside user-facing strings. Review them with the Phase 4 UI copy. |

## Delivery Gate (applicable items)

- R-02 PASS: no em dash (U+2014) under `lib/` (grep). The one en dash is a bullet glyph inside the `BULLET_START` regex, not copy.
- R-15 N/A: no calls to action in this phase.
- R-16 PASS: no buzzwords in the strings; finding 7 is jargon, noted for Phase 4.
- R-17, R-36, R-38: findings 1, 2, and 3. No invented numbers; every number in a message comes from the measured CV.
- BR-07 and StyleGuide § Anti-AI Slop Rules PASS apart from finding 1: no chatbot phrasing, no `**Contoh:**`, and suggestions open with a direct instruction ("Hapus", "Tulis", "Cantumkan", "Pakai", "Ubah", "Buat").
- PDF error messages PASS: each says what happened and what to do next, in one or two sentences.
- Code comments checklist PASS for density and form: no banners, narration, empty labels, emoji, end markers, or vague TODOs; comments state a reason, contract, unit, or edge case. Findings 4 to 6 are accuracy fixes to comments only.
